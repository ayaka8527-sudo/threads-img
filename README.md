# threads-img

Threadsの投稿に添える画像の置き場。`raw.githubusercontent.com` から直接読める必要があるので公開。

- `img/` … 投稿用の画像だけを入れる
- 生成は `~/楽天ROOM/threads/` 側。ここへは `publish_image.py` が push する

**投稿用に作った画像以外は置かない。** 私物の写真や仕事に関わるものは入れない。
